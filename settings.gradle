import org.gradle.api.initialization.resolve.RepositoriesMode

rootProject.name = 'spring-rebac'

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenCentral()
    }
}

include(":core")